document.addEventListener("DOMContentLoaded", (event) =>{

let answer = prompt("What is the \"official\" name of javascript?");

disp = (answer == "ECMAScript") ? "Right!" : "You don't know? \"ECMAScript\"!";

alert(disp);
});
